# Dining Philosophers

This example demonstrates coordinated access to shared fork resources and
includes a Swing visualization. The current application is compiled through
the root CMake project; simulation and resource-loading improvements are
planned in a later refactoring phase.

